types = {
    p: padding,
    m: margin
}
sizes = {
    '0': 0,
    half: 0.5rem,
    '1': 1rem,
    '1-half': 1.5rem,
    '2': 2rem,
    '2-half': 2.5rem,
    '3': 3rem
}

directions = {
    t: top,
    b: bottom,
    l: left,
    r: right
}

:global // @stylint ignore
    for kType, vType in types
        for kSize, vSize in sizes
            // all
            .u-{kType}-{kSize}
                {vType}: vSize !important
            // horizontal
            .u-{kType}h-{kSize}
                {vType}-left: vSize !important
                {vType}-right: vSize  !important
            // vertical
            .u-{kType}v-{kSize}
                {vType}-top: vSize  !important
                {vType}-bottom: vSize  !important
            // each direction
            for kDir, vDir in directions
                .u-{kType}{kDir}-{kSize}
                    {vType}-{vDir}: vSize  !important // @stylint ignore

