@require 'settings/breakpoints'

.SettingCard
    box-sizing border-box
    width 400px
    overflow hidden

    +small-screen()
        width 100%

.SettingCard--clickable
    cursor pointer

.SettingCard--dashed
    color var(--coolGrey)

